---
interface Props {
  className?: string;
  text: string;
}

const { className, text } = Astro.props;
---

<button
  class=`bg-c color-p-w font-s-13 bold font-ls-1 p-t-15 p-b-15 p-r-30 p-l-30 ${className}`
  >{text}</button
>
